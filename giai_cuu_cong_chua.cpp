#include<bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
	// freopen("thu.inp","r",stdin);
	ios_base::sync_with_stdio(0);
	cin.tie(NULL);
	string s;
	getline(cin,s);
	reverse(s.begin(),s.end());
	cout<<s;
}

